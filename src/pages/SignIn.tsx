import * as React           from 'react';


export class SignIn extends React.PureComponent<any, any> {
    render() {
        const { classes } = this.props;
        return (
            <React.Fragment>
               sign in page
            </React.Fragment>
        );
    }
}

